# Secrets
.env

# Build output
target/
*.class

# Downloaded dataset (large, reproducible via data/download_dataset.sh)
data/*.txt
data/*.txt.gz

# Raw per-run result JSON is fine to keep for transparency, but ignore scratch files
results/*.tmp

# OS / IDE
.DS_Store
.idea/
*.iml
.vscode/
